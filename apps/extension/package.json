{
  "name": "jovaltus-extension",
  "version": "0.0.0",
  "private": true,
  "publisher": "jovaltus",
  "displayName": "Jovaltus",
  "description": "歲淵 - VS Code AI coding agent MVP",
  "type": "module",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "package": "vsce package --no-dependencies --allow-missing-repository --out ../../dist/jovaltus-extension.vsix",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@jovaltus/core": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "^1.107.0"
  }
}
